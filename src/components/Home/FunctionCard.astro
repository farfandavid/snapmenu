---
import QRCodeScan from "../assets/QRCodeScan.astro";
import SearchHeart from "../assets/SearchHeart.astro";
import PostcardHeartFill from "../assets/PostcardHeartFill.astro";
import Share from "../assets/Share.astro";
import PencilFill from "../assets/PencilFill.astro";
import Piggy from "../assets/Piggy.astro";

interface Props {
    icon: string;
    title: string;
    description: string;
}

const { icon, title, description }: Props = Astro.props;
---

<div
    class="bg-white shadow-md rounded-lg p-4 gap-1 items-center grid grid-cols-5 hover:shadow-xl hover:-translate-y-1 transition duration-300 linear select-none fade-in"
>
    <div
        class="w-14 h-14 flex items-center justify-center aspect-square bg-orange-500 xl:text-3xl rounded text-white col-span-1"
    >
        {
            () => {
                if (icon === "bi-qr-code-scan") {
                    return <QRCodeScan className="h-8" />;
                } else if (icon === "bi-search-heart") {
                    return <SearchHeart className="h-8" />;
                } else if (icon === "bi-postcard-heart-fill") {
                    return <PostcardHeartFill className="h-8" />;
                } else if (icon === "bi-share-fill") {
                    return <Share className="h-8" />;
                } else if (icon === "bi-pencil-fill") {
                    return <PencilFill className="h-8" />;
                } else if (icon === "bi-piggy-bank-fill") {
                    return <Piggy className="h-8" />;
                }
            }
        }
    </div>
    <div class="w-auto col-span-4">
        <h3 class="xl:text-lg font-bold">{title}</h3>
        <p class="text-gray-700 text-sm">
            {description}
        </p>
    </div>
</div>
